export class Dog {
  constructor(public id,
              public breed,
              public image,
              public description,
              public owner,
              public name,
              public age,
              public ready,
              public gender,
              public location) {
  }
}
